interface:
  display_name: "DXF To GLB"
  short_description: "DXF to GLB with interactive geometry and material tuning."
  default_prompt: "First explicitly ask the user whether to use preview mode (serve) or direct export (build). After the user chooses, execute only that mode and return the resulting paths/URL."
