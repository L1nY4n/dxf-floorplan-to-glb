interface:
  display_name: "DXF To GLB"
  short_description: "DXF to GLB with interactive geometry and material tuning."
  default_prompt: "Start in serve mode by default: launch the local workbench, tune geometry/material interactively, then export grounded GLB/JSON deliverables from the workbench output."
